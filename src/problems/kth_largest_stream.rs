use std::todo;

struct KthLargest {
    k: i32,
    largest_items: Vec<i32>,
    largest: i32,
    smallest: i32,
}

impl KthLargest {
    fn new(k: i32, nums: Vec<i32>) -> Self {
        todo!()
    }

    fn add(&self, val: i32) -> i32 {
        todo!()
    }
}

pub fn run() {
    println!("Bruh");
}
